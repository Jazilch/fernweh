'use es6';

import React from 'react';
import { StyledHeading, StyledSubheading } from '../style';

const HeroText = () => (
  <>
    <StyledHeading>Escape Your Origin</StyledHeading>
    <StyledSubheading>Enter a destination below</StyledSubheading>
  </>
);

export default HeroText;
